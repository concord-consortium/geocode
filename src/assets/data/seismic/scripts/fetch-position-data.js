const axios = require('axios');
const fs = require('fs');
const path = require('path');
require('dotenv').config(); // load .env if needed

const host = "https://gage-data.earthscope.org";
const token = process.env.BEARER_TOKEN;

if (!token) {
  console.error("❌ Missing BEARER_TOKEN in environment.");
  process.exit(1);
}

const dataPathById = id => `/archive/gnss/products/position/${id}/${id}.cwu.nam14.csv`;
const fileNameById = id => `position.${id}.cwu.nam14.csv`;
const writeFileById = id => `src/assets/data/seismic/${fileNameById(id)}`;
const exportFile = "src/assets/data/seismic/position-time-data.js";
const lengthOfHeaderRows = 11;

const stationIds = [
  "HARV",
  "P513",
  "FGST",
  "P519",
  "P518",
  "P521",
  "P535",
  "P537",
  "CRGG",
  "P522",
  "P541",
  "P543",
  "BVPP",
  "P544",
  "P563",
  "P558",
  "P565",
  "P567",
  "P570",
  "P571",
  "P573",
  "P093",
  "P094",
  "P092",
  "CRAM",
  "MIG1",
  "P256",
  "P273",
  "P306",
  "P309",
  "P557",
  "P781",
  "CMOD",
  "P097",
  "P128",
  "P142",
  "P177",
  "P223",
  "P225",
  "P229",
  "P230",
  "P248",
  "P275",
  "P276",
  "P310"
];

(async () => {
  for (const stationId of stationIds) {
    await downloadAndCleanFile(stationId);
  }
  createExportFile();
})();

async function downloadAndCleanFile(stationId) {
  const url = `${host}${dataPathById(stationId)}`;
  const writeFile = writeFileById(stationId);
  const dir = path.dirname(writeFile);

  // Ensure output directory exists
  await fs.promises.mkdir(dir, { recursive: true });

  console.log(`→ Fetching ${stationId} from ${url}`);

  try {
    const response = await axios.get(url, {
      headers: {
        Authorization: `Bearer ${token}`
      },
      responseType: 'stream'
    });

    const writer = fs.createWriteStream(writeFile);

    await new Promise((resolve, reject) => {
      response.data.pipe(writer);
      response.data.on('end', resolve);
      writer.on('error', reject);
    });

    console.log(`✔ Saved raw data to ${writeFile}`);
    await cleanRawData(writeFile);
  } catch (error) {
    console.error(`❌ Failed to fetch ${url}: ${error.message}`);
  }
}

async function cleanRawData(writeFile) {
  try {
    const data = await fs.promises.readFile(writeFile, 'utf-8');
    const lines = data.split('\n');

    const cleaned = lines
      .slice(lengthOfHeaderRows)
      .map(line => {
        const cols = line.split(",");
        return cols.slice(0, 3).map(val => val.trim()).join(",");
      });

    await fs.promises.writeFile(writeFile, cleaned.join('\n'), 'utf-8');
    console.log(`✔ Cleaned: ${writeFile}`);
  } catch (err) {
    console.error(`❌ Error cleaning ${writeFile}: ${err.message}`);
  }
}

function createExportFile() {
  const textArray = [
    "// file generated by fetch-position-data.js",
    ...stationIds.map(id => `import * as ${id} from "./${fileNameById(id)}";`),
    "",
    "const RawPositionTimeData = {",
    ...stationIds.map(id => `  ${id},`),
    "};",
    "",
    "export default RawPositionTimeData;",
  ];

  fs.writeFile(exportFile, textArray.join('\n'), err => {
    if (err) {
      console.error(`❌ Failed to write export file: ${err.message}`);
    } else {
      console.log(`✔ Export file written to ${exportFile}`);
    }
  });
}
