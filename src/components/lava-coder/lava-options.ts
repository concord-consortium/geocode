// This flag controls which elevation map is used for the simulation.
// When true, the larger map that covers almost all of the large island is used.
// When false, the smaller map that only covers the central eastern part of the island is used.
export const useLargeMap = true;

// Set this to true to render the full elevation map extent when the eruption starts.
// This does not impact the simulation, but it's helpful for visualizing what is and is not included in the map.
export const renderMapExtent = true;
